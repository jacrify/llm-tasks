{
  "name": "llm-tasks",
  "version": "1.0.0",
  "description": "Dispatch lines of text as tasks to LLM agents from Obsidian",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "obsidian": "^1.7.2",
    "typescript": "^5.5.0",
    "esbuild": "^0.21.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.14.0"
  }
}
